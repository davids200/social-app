# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type PostMedia {
  id: Int!
  url: String!
  type: String!
}

type Comment {
  id: String!
  content: String!
  userId: String!
  postId: String!
  parentCommentId: String
  createdAt: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Post {
  id: String!
  content: String!
  userId: String!
  locationId: String
  visibility: PostVisibility!
  visibilityLevel: Int
  media: [PostMedia!]
  createdAt: DateTime!
  updatedAt: DateTime!
}

enum PostVisibility {
  PUBLIC
  COUNTRY
  DISTRICT
  SUBCOUNTY
  PARISH
  VILLAGE
  FOLLOWERS
  PRIVATE
}

type UserModel {
  id: String!
  username: String!
  email: String!
}

type Location {
  id: String!
  name: String!
  level: Float!
  parentId: String
}

type AuthResponse {
  accessToken: String!
  refreshToken: String!
}

type UserResponse {
  id: String!
  username: String!
  email: String!
}

type Query {
  users: [UserModel!]!
  getPosts: [Post!]!
  getFeed: [Post!]!
  getLocations(parentId: String): [Location!]!
  getLocationChain(locationId: String!): [Location!]!
  getPostComments(postId: String!): [Comment!]!
  getCommentTree(postId: String!): [Comment!]!
  likeCount(postId: String!): Float!
}

type Mutation {
  createUser(username: String!, email: String!, password: String!): UserModel!
  createPost(content: String!, visibility: String): Post!
  createLocation(input: CreateLocationInput!): Location!
  createComment(postId: String!, content: String!, parentCommentId: String): Comment!
  register(username: String!, email: String!, password: String!): UserResponse!
  login(email: String!, password: String!): AuthResponse!
  likePost(postId: String!): Boolean!
  unlikePost(postId: String!): Boolean!
  follow(input: FollowDto!): Boolean!
  unfollow(input: FollowDto!): Boolean!
}

input CreateLocationInput {
  name: String!
  level: Int!
  parentId: String
}

input FollowDto {
  targetUserId: String!
}