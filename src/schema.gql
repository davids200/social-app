# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type User {
  id: Int!
  username: String!
  email: String!
}

type AuthResponse {
  accessToken: String!
  refreshToken: String!
}

type UserResponse {
  id: Int!
  username: String!
  email: String!
}

type Post {
  id: Int!
  content: String!
  userId: Int!
  createdAt: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Like {
  id: Int!
  userId: Int!
  postId: Int
  commentId: Int
}

type LikeResponse {
  postId: Int
  commentId: Int
  likeCount: Int!
  likedByMe: Boolean!
}

type Query {
  users: [User!]!
  getPosts: [Post!]!
  postLikeCount(postId: Int!): Int!
  commentLikeCount(commentId: Int!): Int!
}

type Mutation {
  createUser(username: String!, email: String!): User!
  register(username: String!, email: String!, password: String!): UserResponse!
  login(email: String!, password: String!): AuthResponse!
  createPost(userId: Int!, content: String!): Post!
  likePost(userId: Int!, postId: Int!): LikeResponse!
  likeComment(userId: Int!, commentId: Int!): Like!
  unlikePost(userId: Int!, postId: Int!): LikeResponse!
  unlikeComment(userId: Int!, commentId: Int!): LikeResponse!
}